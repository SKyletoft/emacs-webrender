if window_system != 'w32'
  subdir_done()
endif

supported_features = [
  'svg', 'webp', 'imagemagick', 'gif',
  'jpeg', 'tiff', 'xpm', 'png', 'native-image-api',
  'toolkit-scroll-bars',
]

term_header='w32term.h'
